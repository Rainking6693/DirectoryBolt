[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # While debugging Tailwind in devDependencies:
  NODE_ENV = "development"

# Let Netlify’s Next.js runtime handle routing/build
[[plugins]]
  package = "@netlify/plugin-nextjs"
