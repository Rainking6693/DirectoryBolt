# DirectoryBolt Environment Configuration
# =============================================================================
# CORE URLS / APP
# =============================================================================
# Used by server-side routes and redirects
BASE_URL=http://localhost:3000
# Public app URL (used by client code)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Used by NextAuth if you use it
NEXTAUTH_URL=http://localhost:3000
# Optional canonical site URLs (prod deploys usually set these in Netlify)
SITE_URL=http://localhost:3000
PRODUCTION_URL=
URL=
DEPLOY_URL=

# Port for local dev servers
PORT=3000

# Public API base (exposed to browser; do not put secrets here)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Optional build-time stamp (string)
NEXT_PUBLIC_BUILD_TIME=

# =============================================================================
# SECURITY / AUTH
# =============================================================================
JWT_SECRET=change_me_in_prod
ALLOWED_ORIGINS=http://localhost:3000
ADMIN_API_KEY=
ADMIN_SEED_KEY=
INSTANCE_ID=

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# Secret keys (server only)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# Some code paths use STRIPE_SECRET instead of STRIPE_SECRET_KEY; keep both mapped
STRIPE_SECRET=sk_test_your_stripe_secret_key_here

# Publishable (browser safe)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# One-time prices (examples; replace with actual IDs from Stripe)
STRIPE_STARTER_PRICE_ID=price_starter_50_directories_4900_cents
STRIPE_GROWTH_PRICE_ID=price_growth_100_directories_8900_cents
STRIPE_PRO_PRICE_ID=price_pro_200_directories_15900_cents

# One-time purchase price
STRIPE_PURCHASE_PRICE_ID=price_one_time_purchase_replace_with_actual_id
STRIPE_AUTO_UPDATE_PRICE_ID=price_auto_update_4900_cents_one_time

# Add-ons (examples)
STRIPE_FAST_TRACK_PRICE_ID=price_fast_track_2500_cents
STRIPE_PREMIUM_DIRECTORIES_PRICE_ID=price_premium_directories_1500_cents
STRIPE_MANUAL_QA_PRICE_ID=price_manual_qa_1000_cents
STRIPE_CSV_EXPORT_PRICE_ID=price_csv_export_900_cents

# Webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Legacy/back-compat
STRIPE_PROFESSIONAL_PRICE_ID=price_professional_replace_with_actual_id
STRIPE_ENTERPRISE_PRICE_ID=price_enterprise_replace_with_actual_id

# =============================================================================
# AIRTABLE (PAT required)
# =============================================================================
# Your file had AIRTABLE_ACCESS_TOKEN; some code paths look for AIRTABLE_API_TOKEN.
# Keep both so either path works; set both to your PAT in .env.local/Netlify.
AIRTABLE_ACCESS_TOKEN=pat_xxxxxxxxxxxxxxxxxxxxx
AIRTABLE_API_TOKEN=pat_xxxxxxxxxxxxxxxxxxxxx
AIRTABLE_BASE_ID=appZDNMzebkaOkLXo
AIRTABLE_TABLE_NAME=Directory Bolt Import

# =============================================================================
# SUPABASE (optional)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-key-here

# =============================================================================
# AI / ANALYTICS / MONITORING (optional)
# =============================================================================
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
SENTRY_DSN=https://your-sentry-dsn-here
GA_MEASUREMENT_ID=G-XXXXXXXXXX
PAGESPEED_API_KEY=

# =============================================================================
# PUPPETEER CONFIGURATION (for scraping)
# =============================================================================
# Path to chromium executable for serverless environments
PUPPETEER_EXECUTABLE_PATH=

# =============================================================================
# RUNTIME / TESTING / OBSERVABILITY (optional)
# =============================================================================
HEADLESS=
BUILD_MODE=
VERBOSE=
CI=
NETLIFY=
BUILDING=
GITHUB_SHA=
GITHUB_RUN_NUMBER=

TEST_BASE_URL=
REAL_REQUESTS=
TEST_TIMEOUT=
SAVE_RESULTS=
INTERACTIVE=
OUTPUT_FILE=
OUTPUT_DIR=
RUN_EXTERNAL=
PARALLEL=
TIMEOUT=

# Observability / monitoring toggles (if you use them)
MONITOR_INTERVAL=
ALERT_THRESHOLD=
WEBSOCKET_URL=
TRACKING_DASHBOARD_URL=
TRACKING_API_TOKEN=

# =============================================================================
# USER AGENT (optional)
# =============================================================================
USER_AGENT=DirectoryBolt/2.0 (+https://your-domain.com)

# =============================================================================
# ENV / LOGGING
# =============================================================================
NODE_ENV=development
DEBUG=true
LOG_LEVEL=info
